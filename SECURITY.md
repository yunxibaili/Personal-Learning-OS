# Security Policy · 安全策略

## 报告漏洞

通过 GitHub Issues（标记 `security`）或仓库所有者联系方式私下报告。
**请不要在公开 Issue 中附带你本地的笔记内容、API key 或数据库文件。**

## 安全设计（本项目作者的强制约束，详见 [AGENTS.md §19](AGENTS.md)）

- **零遥测**：无统计、无崩溃上报、无匿名分析、无"检查更新"外呼
- **只绑回环**：后端仅监听 `127.0.0.1`，禁止 `0.0.0.0`（防局域网暴露）
- **无出站即无风险**：唯一出站白名单 = 用户在设置中显式配置的 LLM `base_url`
  （OpenAI-compatible）。白名单之外一切出站请求（更新检查、CDN、统计）一律不做
- **上下文最小化**：发给云端 LLM 的内容仅限用户问题 + 检索片段 + 显式授权的笔记；
  `.env`、API 密钥、数据库文件等在代码层硬过滤，绝不外发
- **本地构建**：前端产物完全本地化，不引用任何 CDN

## 支持的版本

| 分支 | 状态 |
|---|---|
| `main` | ✅ 接收安全修复 |
